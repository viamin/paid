# PostgreSQL-backed Action Cable for cross-process broadcasting.
# No Redis needed — aligned with the existing PostgreSQL architecture.
development:
  adapter: postgresql

test:
  adapter: test

production:
  adapter: solid_cable
  connects_to:
    database:
      writing: cable
  polling_interval: 0.1.seconds
  message_retention: 1.day
