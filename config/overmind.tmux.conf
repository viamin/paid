# Keep tmux history and paste buffers bounded so runaway output is easier to reason about.
set -g history-limit 50000
set -g buffer-limit 20

# Let scroll wheel input enter copy mode and scroll session logs when attached
# to Overmind's tmux session inside the devcontainer.
set -g mouse on
