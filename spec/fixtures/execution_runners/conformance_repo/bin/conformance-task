#!/usr/bin/env bash
set -euo pipefail

# fixture_version must match ExecutionRunners::ConformanceSuite::FIXTURE_VERSION.
mkdir -p artifacts
cat > artifacts/conformance-result.json <<'JSON'
{"status":"ok","token":"CONFORMANCE_OK","fixture_version":1}
JSON

printf 'CONFORMANCE_OK\n'
