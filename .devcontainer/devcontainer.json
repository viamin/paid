// For format details, see https://containers.dev/implementors/json_reference/.
// For config options, see the README at: https://github.com/devcontainers/templates/tree/main/src/ruby
{
  "name": "paid",
  "dockerComposeFile": "compose.yaml",
  "service": "rails-app",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",

  // Features to add to the dev container. More info: https://containers.dev/features.
  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {},
    "ghcr.io/devcontainers/features/node:1": {},
    "ghcr.io/devcontainers/features/go:1": {},
    "ghcr.io/devcontainers/features/docker-outside-of-docker:1": {"moby":false},
    "ghcr.io/rails/devcontainer/features/postgres-client": {}
  },

  "containerEnv": {
    "KAMAL_REGISTRY_PASSWORD": "$KAMAL_REGISTRY_PASSWORD",
    "DB_HOST": "postgres"
  },

  "remoteEnv": {
    "PATH": "${containerEnv:PATH}:/home/vscode/go/bin"
  },

  // Use 'forwardPorts' to make a list of ports inside the container available locally.
  "forwardPorts": [3000, 5432],

  // Mount host's Claude Code and GitHub CLI auth to persist between rebuilds
  "mounts": [
    "source=${localEnv:HOME}/.claude,target=/home/vscode/.claude,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.config/gh,target=/home/vscode/.config/gh,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.gitconfig,target=/home/vscode/.gitconfig,type=bind,consistency=cached"
  ],

  // Configure tool-specific properties.
  // "customizations": {},

  // Uncomment to connect as root instead. More info: https://containers.dev/implementors/json_reference/#remoteUser.
  "remoteUser": "vscode",
  "containerUser": "vscode",


  // Use 'postCreateCommand' to run commands after the container is created.
  "postCreateCommand": {
    "bundler": "gem install bundler:2.7.2",
    "corepack": "corepack enable && corepack prepare yarn@stable --activate",
    "claude-code": "npm install -g @anthropic-ai/claude-code",
    "multiclaude": "go install github.com/dlorenc/multiclaude/cmd/multiclaude@latest",
    "markdownlint": "npm install -g markdownlint-cli"
  },

  // Use 'postStartCommand' to run commands every time the container starts.
  "postStartCommand": {
    "setup": "until pg_isready -h postgres -U postgres -q; do sleep 1; done && bin/setup --skip-server",
    "multiclaude-start": "multiclaude start && sleep 2 && multiclaude repo init https://github.com/viamin/paid"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "Anthropic.claude-code"
      ]
    }
  }
}
