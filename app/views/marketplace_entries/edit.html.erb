<% content_for(:title) { "Edit Marketplace Entry - Paid" } %>

<div class="mx-auto max-w-4xl px-4 py-8 sm:px-6 lg:px-8">
  <h1 class="text-2xl font-semibold text-gray-900">Edit Marketplace Entry</h1>
  <p class="mt-1 text-sm text-gray-500">Updating runtime content creates a new immutable version and moves the entry to it automatically.</p>
  <div class="mt-6 rounded-lg bg-white p-6 shadow">
    <%= render "form", marketplace_entry: @marketplace_entry %>
  </div>
</div>
