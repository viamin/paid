<div class="rounded-lg border border-gray-200 bg-white p-6 shadow-sm">
  <div class="flex items-center justify-between gap-4">
    <div>
      <h3 class="text-base font-semibold text-gray-900">Upcoming Queue</h3>
      <p class="mt-1 text-sm text-gray-500">Queued runs for your projects in approximate scheduler priority order.</p>
    </div>
  </div>

  <% if queue_preview.any? %>
    <div class="mt-4 flow-root">
      <div class="-mx-4 -my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
        <div class="inline-block min-w-full py-2 align-middle sm:px-6 lg:px-8">
          <table class="min-w-full divide-y divide-gray-300">
            <thead>
              <tr>
                <th scope="col" class="py-2 pl-4 pr-3 text-left text-xs font-medium uppercase tracking-wide text-gray-500 sm:pl-0">Pos</th>
                <th scope="col" class="px-3 py-2 text-left text-xs font-medium uppercase tracking-wide text-gray-500">Project</th>
                <th scope="col" class="px-3 py-2 text-left text-xs font-medium uppercase tracking-wide text-gray-500">Priority</th>
                <th scope="col" class="px-3 py-2 text-left text-xs font-medium uppercase tracking-wide text-gray-500">Goal</th>
                <th scope="col" class="px-3 py-2 text-left text-xs font-medium uppercase tracking-wide text-gray-500">Created</th>
                <th scope="col" class="px-3 py-2 text-left text-xs font-medium uppercase tracking-wide text-gray-500">Waiting</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-200 bg-white">
              <% queue_preview.each do |entry| %>
                <tr>
                  <td class="whitespace-nowrap py-3 pl-4 pr-3 text-sm font-medium text-gray-900 sm:pl-0"><%= entry.position %></td>
                  <td class="whitespace-nowrap px-3 py-3 text-sm text-gray-900"><%= entry.run.project.full_name %></td>
                  <td class="whitespace-nowrap px-3 py-3 text-sm text-gray-500"><%= agent_run_priority_badge(entry.run) %></td>
                  <td class="whitespace-nowrap px-3 py-3 text-sm text-gray-500"><%= goal_label(entry.run.goal) %></td>
                  <td class="whitespace-nowrap px-3 py-3 text-sm text-gray-500"><%= local_time(entry.run.created_at, format: :short) %></td>
                  <td class="px-3 py-3 text-sm text-gray-500"><%= entry.waiting_reason %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  <% else %>
    <p class="mt-4 text-sm text-gray-500">No queued runs for your projects.</p>
  <% end %>
</div>
