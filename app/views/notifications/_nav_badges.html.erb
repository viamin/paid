<% counts = local_assigns.key?(:badge_counts) ? badge_counts : notification_badge_counts %>
<div id="notification_nav_badges"
     data-controller="nav-badges"
     data-nav-badges-counts-value="<%= counts.to_json %>">
</div>
