<li id="<%= dom_id(issue) %>" class="flex items-center justify-between px-4 py-3">
  <div class="min-w-0 flex-1">
    <div class="flex items-center space-x-2">
      <%= link_to "##{issue.github_number}",
            issue.github_url,
            target: "_blank",
            rel: "noopener noreferrer",
            class: "flex-shrink-0 text-sm font-medium text-indigo-600 hover:text-indigo-900" %>
      <span class="truncate text-sm text-gray-900"><%= issue.title %></span>
    </div>
    <% if issue.labels.any? %>
      <div class="mt-1 flex flex-wrap gap-1">
        <% issue.labels.each do |label| %>
          <span class="inline-flex items-center rounded-full bg-gray-100 px-2 py-0.5 text-xs text-gray-600"><%= label %></span>
        <% end %>
      </div>
    <% end %>
  </div>
  <div class="ml-4 flex flex-shrink-0 items-center space-x-2">
    <%= paid_state_badge(issue.paid_state) %>
    <%= link_to "Run", new_project_agent_run_path(project, issue_id: issue.id), class: "text-xs font-medium text-indigo-600 hover:text-indigo-900", data: { action_button: true } %>
  </div>
</li>
