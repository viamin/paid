name: Claude Code

# Triggers: Claude runs when @claude is mentioned in issue/PR comments,
# PR review comments, PR reviews, or newly opened issues. A separate
# verify job checks actual write access with minimal permissions before
# the main claude job runs with elevated permissions. This two-job
# design limits exposure of write-scoped tokens to verified actors.
on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened]
  pull_request_review:
    types: [submitted]

# Minimal default permissions — each job declares only what it needs.
permissions: {}

jobs:
  verify:
    # Lightweight gate: check that the actor actually has write access.
    # Runs with read-only permissions so even if an untrusted actor
    # somehow passes the `if` filter, no write token is exposed.
    if: |
      (
        github.event_name == 'issue_comment' &&
        contains(toLower(github.event.comment.body), '@claude') &&
        (
          github.event.comment.author_association == 'OWNER' ||
          github.event.comment.author_association == 'MEMBER' ||
          github.event.comment.author_association == 'COLLABORATOR'
        )
      ) ||
      (
        github.event_name == 'pull_request_review_comment' &&
        contains(toLower(github.event.comment.body), '@claude') &&
        (
          github.event.comment.author_association == 'OWNER' ||
          github.event.comment.author_association == 'MEMBER' ||
          github.event.comment.author_association == 'COLLABORATOR'
        )
      ) ||
      (
        github.event_name == 'pull_request_review' &&
        contains(toLower(github.event.review.body || ''), '@claude') &&
        (
          github.event.review.author_association == 'OWNER' ||
          github.event.review.author_association == 'MEMBER' ||
          github.event.review.author_association == 'COLLABORATOR'
        )
      ) ||
      (
        github.event_name == 'issues' &&
        (
          contains(toLower(github.event.issue.body || ''), '@claude') ||
          contains(toLower(github.event.issue.title), '@claude')
        ) &&
        (
          github.event.issue.author_association == 'OWNER' ||
          github.event.issue.author_association == 'MEMBER' ||
          github.event.issue.author_association == 'COLLABORATOR'
        )
      )
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      allowed: ${{ steps.check-permissions.outputs.allowed }}
    steps:
      - name: Check actor repository permission
        id: check-permissions
        env:
          GH_TOKEN: ${{ github.token }}
          ACTOR: ${{ github.actor }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ -z "$ACTOR" ]; then
            echo "::error::Could not determine actor"
            exit 1
          fi
          # Query the actor's repository permission. Handle failures and missing
          # permissions gracefully so unauthorized users cause a no-op instead
          # of failing the workflow.
          if ! PERM=$(gh api "repos/${REPO}/collaborators/${ACTOR}/permission" --jq '.permission' 2>/dev/null); then
            echo "::notice::Could not determine permission for ${ACTOR}; treating as no access"
            PERM=""
          fi
          echo "User ${ACTOR} has permission: ${PERM:-<none>}"
          if [[ "$PERM" == "admin" || "$PERM" == "maintain" || "$PERM" == "write" ]]; then
            echo "allowed=true" >> "$GITHUB_OUTPUT"
          else
            echo "::notice::User ${ACTOR} does not have write access (has: ${PERM:-<none>})"
            echo "allowed=false" >> "$GITHUB_OUTPUT"
          fi

  claude:
    needs: verify
    # Defense-in-depth: require both the verify job gate AND author_association
    # checks directly on this privileged job. Even if the verify output were
    # somehow bypassed, this condition independently rejects unauthorized actors.
    # The author_association checks also make the authorization visible to static
    # analysis tools (CodeQL js/actions/checkout-of-untrusted-code).
    if: |
      needs.verify.outputs.allowed == 'true' &&
      (
        github.event.comment.author_association == 'OWNER' ||
        github.event.comment.author_association == 'MEMBER' ||
        github.event.comment.author_association == 'COLLABORATOR' ||
        github.event.review.author_association == 'OWNER' ||
        github.event.review.author_association == 'MEMBER' ||
        github.event.review.author_association == 'COLLABORATOR' ||
        github.event.issue.author_association == 'OWNER' ||
        github.event.issue.author_association == 'MEMBER' ||
        github.event.issue.author_association == 'COLLABORATOR'
      )
    runs-on: ubuntu-latest
    # Claude Code requires write access to create comments, branches, and
    # commits. id-token:write is needed for OIDC authentication.
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read
    steps:
      - name: Re-verify actor permission
        id: reverify
        # Mitigates TOCTOU: re-check write access at execution time, not
        # just at the verify-job gate. If permissions were revoked between
        # jobs, this step prevents any write-scoped action from running.
        # Includes retry/backoff and fail-closed behavior so transient API
        # failures skip Claude without failing the entire workflow.
        env:
          GH_TOKEN: ${{ github.token }}
          ACTOR: ${{ github.actor }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail

          PERM=""
          attempts=5

          for i in $(seq 1 "${attempts}"); do
            if PERM=$(gh api "repos/${REPO}/collaborators/${ACTOR}/permission" --jq '.permission' 2>/dev/null); then
              break
            fi

            if [[ "${i}" -lt "${attempts}" ]]; then
              # Exponential backoff: 1s, 2s, 4s, 8s...
              sleep $((2 ** (i - 1)))
            fi
          done

          if [[ -z "${PERM:-}" ]]; then
            echo "allowed=false" >> "${GITHUB_OUTPUT}"
            echo "permission=unknown" >> "${GITHUB_OUTPUT}"
            echo "::warning::Failed to verify actor ${ACTOR} permission due to GitHub API errors; skipping Claude run."
            exit 0
          fi

          if [[ "${PERM}" == "admin" || "${PERM}" == "maintain" || "${PERM}" == "write" ]]; then
            echo "allowed=true" >> "${GITHUB_OUTPUT}"
            echo "permission=${PERM}" >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          echo "allowed=false" >> "${GITHUB_OUTPUT}"
          echo "permission=${PERM}" >> "${GITHUB_OUTPUT}"
          echo "::warning::Actor ${ACTOR} no longer has write access (has: ${PERM}); skipping Claude run."
          exit 0

      - name: Run Claude Code
        id: claude
        if: steps.reverify.outputs.allowed == 'true'
        # CodeQL: untrusted-checkout-toctou / checkout-of-untrusted-code
        # No explicit actions/checkout step — claude-code-action handles
        # its own repository access internally, including reading config
        # files (CLAUDE.md) and PR diffs via the GitHub API. Residual
        # CodeQL alerts on this step cannot be resolved in this workflow
        # because the action internally accesses PR refs as core
        # functionality. Mitigations in place:
        #   1. verify job gates on author_association + gh API permission
        #   2. Job-level author_association condition (defense-in-depth)
        #   3. Re-verify step with retry/backoff re-checks write access
        #      immediately before this step runs
        #   4. persist-credentials: false on any checkout steps
        #   5. The action performs its own permission checks before
        #      accessing PR code
        uses: anthropics/claude-code-action@26ec041249acb0a944c0a47b6c0c13f05dbc5b44 # v1
        with:
          # IMPORTANT: Authentication uses an OAuth token, NOT an API key.
          # Create the CLAUDE_CODE_OAUTH_TOKEN secret (not an Anthropic API key)
          # in Settings > Secrets and variables > Actions before enabling this workflow.
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

          additional_permissions: |
            actions: read
