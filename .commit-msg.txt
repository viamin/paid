fix: harden paid-paused sync for synthetic issues and scope PR copy

Address review feedback on #2636:

- Skip the GitHub label push for synthetic code-scanning/Dependabot
  issues (no backing GitHub issue), so pausing them no longer 404s into
  a 500. The local paused flag still excludes them from auto-pick.
- Scope the "automation will not pick this up" promise to issues, since
  paused only gates issue auto-pick today (PR review/escalation gating
  is a tracked follow-up). Updated the column comment + schema, the
  paid-paused GitHub label description, and the pause-toggle confirm
  and tooltip copy so PRs are not promised automation will stop.
