# Paid Development Environment Variables
#
# NOTE: When using docker compose, the following variables are configured
# directly in docker-compose.yml and are NOT read from this .env file.
# This file serves as documentation of the expected environment variables
# and for use with local (non-Docker) development.

# Database connection
DATABASE_URL=postgres://paid:paid@localhost:5432/paid_development

# Rails environment
RAILS_ENV=development

# Temporal server address (used by Rails app to connect to Temporal)
TEMPORAL_HOST=localhost:7233

# Temporal UI base URL (used for workflow monitoring links)
TEMPORAL_UI_URL=http://localhost:8080
